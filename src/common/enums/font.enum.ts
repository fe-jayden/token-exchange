export enum FONT_FAMILY_ENUM {
  PRIMARY = "'Inter', sans-serif",
  SECONDARY = "'AnotherFont', serif",
  TERTIARY = "'TERTIARY'",
  QUATERNARY = "'QUATERNARY'"
}

export enum FONT_SIZE_ENUM {
  DEFAULT = 16,
  MOBILE = 15,
  TABLET = 15,
  DESKTOP = 15
}
